<view class="page-wrap route-page" wx:if="{{loaded}}">

  <view class="route-header">
    <view class="ai-badge">
      <text class="ai-badge-text">{{routeSourceLabel}}</text>
    </view>
    <text class="route-title">开放展厅目录</text>
    <text class="route-subtitle">先查看当前开放展厅；真实地图和参观动线将在馆方数据接入后生成。</text>
    <text wx:if="{{routeNotice}}" class="route-notice">{{routeNotice}}</text>
  </view>

  <view class="stats-row">
    <view class="stat-block">
      <text class="stat-label">目录总时长</text>
      <text class="stat-value">{{totalDesc}}</text>
    </view>
    <view class="stat-sep"></view>
    <view class="stat-block">
      <text class="stat-label">当前人格</text>
      <text class="stat-value">{{personaLabel}}</text>
    </view>
  </view>

  <view class="steps-section">
    <text class="section-label">展厅列表</text>
    <view class="steps-list">
      <view
        wx:for="{{steps}}"
        wx:key="hallId"
        class="step-wrap"
        style="animation-delay: {{index * 80}}ms"
      >
        <view class="step-rail">
          <view class="step-node step-node-upcoming">
            <text class="step-node-num">{{item.order}}</text>
          </view>
          <view
            wx:if="{{index < stepsCount - 1}}"
            class="step-conn-line step-conn-line-upcoming"
          ></view>
        </view>

        <view class="step-card step-card-upcoming">
          <view class="step-status-badge step-status-badge-upcoming">
            <text class="step-status-text step-status-text-upcoming">展厅 {{item.order}}</text>
          </view>

          <view class="step-title-row">
            <text class="step-hall-name step-hall-name-upcoming">{{item.name}}</text>
            <text class="step-duration">{{item.duration}}</text>
          </view>

          <view class="step-reason-block">
            <text class="step-reason-label">简介</text>
            <text class="step-reason-text">{{item.reason}}</text>
          </view>

          <view class="step-focus-row" wx:if="{{item.focus}}">
            <text class="step-focus-label">重点关注</text>
            <text class="step-focus-text">{{item.focus}}</text>
          </view>

          <view class="step-highlights">
            <text
              wx:for="{{item.highlights}}"
              wx:key="*this"
              wx:for-item="h"
              class="step-tag step-tag-upcoming"
            >{{h}}</text>
          </view>
        </view>
      </view>
    </view>
  </view>

  <view class="bottom-action">
    <button class="btn-primary" bindtap="startTour">选择展厅</button>
  </view>
</view>
