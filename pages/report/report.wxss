/* ── Page wrapper ─────────────────────────────────────────────────────────── */

.report {
  min-height: 100vh;
  padding-bottom: calc(60rpx + constant(safe-area-inset-bottom));
  padding-bottom: calc(60rpx + env(safe-area-inset-bottom));
}

.report-scroll {
  height: 100vh;
}

/* ── Loading screen ───────────────────────────────────────────────────────── */

.report-loading {
  height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 28rpx;
  background: #F8F3EA;
}

.report-loading-icon {
  font-size: 80rpx;
  line-height: 1;
}

.report-loading-dots {
  display: flex;
  gap: 14rpx;
}

.rdot {
  width: 14rpx;
  height: 14rpx;
  border-radius: 50%;
  background: #A85732;
  animation: rdot-blink 1.2s infinite;
}

.rdot:nth-child(2) { animation-delay: 0.2s; }
.rdot:nth-child(3) { animation-delay: 0.4s; }

@keyframes rdot-blink {
  0%, 80%, 100% { opacity: 0.2; }
  40%            { opacity: 1; }
}

.report-loading-text {
  font-size: 28rpx;
  color: #8A6E55;
}

/* ── Header ───────────────────────────────────────────────────────────────── */

.report-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 40rpx 0 40rpx;
}

.report-badge-icon {
  font-size: 88rpx;
  line-height: 1;
  margin-bottom: 20rpx;
}

.report-title {
  font-size: 40rpx;
  font-weight: 700;
  color: #3F2A1D;
  margin-bottom: 12rpx;
  text-align: center;
}

.report-persona {
  font-size: 28rpx;
  letter-spacing: 2rpx;
}

/* ── Stats grid ───────────────────────────────────────────────────────────── */

.stats-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  background: #F3EADD;
  border-radius: 20rpx;
  padding: 32rpx 16rpx;
  margin-bottom: 32rpx;
  text-align: center;
}

.stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.stat-num {
  font-size: 36rpx;
  font-weight: 700;
  color: #3F2A1D;
  display: block;
  margin-bottom: 8rpx;
}

.stat-label {
  font-size: 22rpx;
  display: block;
}

/* ── Section label ────────────────────────────────────────────────────────── */

.section-label {
  font-size: 28rpx;
  display: block;
  margin-bottom: 24rpx;
}

.summary-card {
  background: #FFFDF9;
}

.summary-text {
  display: block;
  font-size: 28rpx;
  color: #5A3E2B;
  line-height: 1.75;
}

.reflection-card {
  background: #FFFDF9;
  border-color: #E7D7C5;
  box-shadow: 0 10rpx 24rpx rgba(63, 42, 29, 0.04);
}

.reflection-row {
  display: grid;
  grid-template-columns: 76rpx 1fr;
  gap: 18rpx;
  padding: 20rpx;
  margin-top: 14rpx;
  border: 2rpx solid #EAD9C9;
  border-radius: 14rpx;
  background: #FFF9F1;
}

.reflection-row:first-of-type {
  margin-top: 0;
}

.reflection-label {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 40rpx;
  border-radius: 20rpx;
  background: #F2E3D2;
  font-size: 22rpx;
  font-weight: 700;
  color: #A85732;
  line-height: 40rpx;
  text-align: center;
}

.reflection-text {
  font-size: 26rpx;
  color: #5A3E2B;
  line-height: 1.65;
}

.hall-card-list {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

.hall-summary-row {
  padding: 22rpx 24rpx;
  background: #FFF9F1;
  border-radius: 14rpx;
  border: 2rpx solid #EAD9C9;
}

.hall-summary-name {
  display: block;
  font-size: 28rpx;
  font-weight: 700;
  color: #3F2A1D;
  margin-bottom: 8rpx;
}

.hall-summary-note {
  display: block;
  font-size: 25rpx;
  color: #8A6E55;
  line-height: 1.55;
}

.question-list {
  margin-top: 24rpx;
  display: flex;
  flex-direction: column;
  gap: 14rpx;
}

.question-row {
  display: flex;
  align-items: flex-start;
  gap: 14rpx;
  padding: 18rpx 20rpx;
  background: #F3EADD;
  border-radius: 14rpx;
}

.question-mark {
  width: 40rpx;
  height: 40rpx;
  border-radius: 20rpx;
  background: #A85732;
  color: #FFF9F1;
  font-size: 22rpx;
  line-height: 40rpx;
  text-align: center;
  flex-shrink: 0;
}

.question-text {
  flex: 1;
  color: #5A3E2B;
  font-size: 26rpx;
  line-height: 1.6;
}

/* ── Review checklist ─────────────────────────────────────────────────────── */

.review-list {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

.review-row {
  padding: 22rpx 24rpx;
  background: #FFF9F1;
  border-radius: 14rpx;
  border: 2rpx solid #EAD9C9;
}

.review-label {
  display: block;
  font-size: 26rpx;
  font-weight: 700;
  color: #A85732;
  margin-bottom: 8rpx;
}

.review-text {
  display: block;
  font-size: 26rpx;
  color: #5A3E2B;
  line-height: 1.6;
}

/* ── Highlights ───────────────────────────────────────────────────────────── */

.highlight-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.highlight-row {
  display: flex;
  align-items: flex-start;
  gap: 16rpx;
}

.highlight-dot {
  font-size: 18rpx;
  margin-top: 8rpx;
  flex-shrink: 0;
}

.highlight-text {
  font-size: 28rpx;
  color: #5A3E2B;
  line-height: 1.6;
  flex: 1;
}

.compact-card {
  background: #F8F2EA;
}

.record-note-list {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

.record-note {
  padding: 20rpx 22rpx;
  background: #FFF9F1;
  border-radius: 14rpx;
  border: 2rpx solid #EAD9C9;
}

.record-point {
  display: block;
  font-size: 25rpx;
  color: #5A3E2B;
  line-height: 1.65;
}

/* ── Error notice (fallback indicator) ───────────────────────────────────── */

.error-notice {
  background: #FFF9F1;
  border-left: 6rpx solid #A85732;
  border-radius: 0 14rpx 14rpx 0;
  padding: 18rpx 22rpx;
  margin-bottom: 24rpx;
}

.error-notice-text {
  font-size: 24rpx;
  color: #B08060;
  display: block;
  line-height: 1.6;
}

/* ── Actions ──────────────────────────────────────────────────────────────── */

.report-actions {
  padding: 16rpx 0 calc(60rpx + constant(safe-area-inset-bottom));
  padding: 16rpx 0 calc(60rpx + env(safe-area-inset-bottom));
}
